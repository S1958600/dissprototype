To operate the marking tool, run main_controller.py and the interface should appear. 

Several debug images are still given when running the image processing. 
The images that the system returns can be changed by commenting out the cv.imshow() commands in image_controller.py